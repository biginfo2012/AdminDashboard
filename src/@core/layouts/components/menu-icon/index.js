const MenuIcon = props => {
    // ** Props
    const { iconImg } = props

    return (
        <img
            className=''
            src={iconImg}
            width='20'
            height='20'
            alt='iconImg'
        />
    )
}

export default MenuIcon
